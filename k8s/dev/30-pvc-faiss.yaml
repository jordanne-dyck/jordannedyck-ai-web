apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: faiss-db
  namespace: jordbot-dev
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: nfs-csi
  resources:
    requests:
      storage: 500Mi
