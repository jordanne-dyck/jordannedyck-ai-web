apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: jordbot
spec:
  to:
    kind: Service
    name: jordannedyck-ai-web
  port:
    targetPort: 3000
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
